import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Progress } from "@/components/ui/progress";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Clock3, Music2, Plus, WalletCards } from "lucide-react";
import { Link } from "wouter";

export default function AppDashboard() {
  const { data, isLoading } = trpc.dashboard.overview.useQuery();
  return <DashboardLayout><div className="mx-auto max-w-6xl space-y-8"><section className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-amber-300">Tableau de bord</p><h1 className="mt-3 font-serif text-4xl tracking-[-.04em] text-white">Bonjour, créateur.</h1><p className="mt-2 text-zinc-400">Votre prochain morceau n’attend qu’une direction.</p></div><Link href="/creer"><Button className="rounded-full bg-amber-300 text-zinc-950 hover:bg-amber-200"><Plus className="mr-2 h-4 w-4" /> Nouvelle création</Button></Link></section><section className="grid gap-4 md:grid-cols-3"><div className="rounded-3xl border border-amber-300/20 bg-gradient-to-br from-amber-300/20 to-orange-900/10 p-6"><WalletCards className="h-5 w-5 text-amber-300" /><p className="mt-9 text-4xl font-semibold tracking-tight">{isLoading?"—":data?.wallet.balance ?? 0}</p><p className="mt-1 text-sm text-zinc-400">crédits disponibles</p><Link href="/credits" className="mt-6 inline-flex items-center text-sm text-amber-200">Recharger <ArrowUpRight className="ml-1 h-4 w-4" /></Link></div><div className="rounded-3xl border border-white/10 bg-white/[.03] p-6"><Music2 className="h-5 w-5 text-violet-300" /><p className="mt-9 text-4xl font-semibold tracking-tight">{isLoading?"—":data?.generations.length ?? 0}</p><p className="mt-1 text-sm text-zinc-400">créations récentes</p><Link href="/bibliotheque" className="mt-6 inline-flex items-center text-sm text-zinc-300">Voir la bibliothèque <ArrowUpRight className="ml-1 h-4 w-4" /></Link></div><div className="rounded-3xl border border-white/10 bg-white/[.03] p-6"><Clock3 className="h-5 w-5 text-emerald-300" /><p className="mt-9 text-4xl font-semibold tracking-tight">{isLoading?"—":data?.generations.filter(g=>g.status==="completed").length ?? 0}</p><p className="mt-1 text-sm text-zinc-400">morceaux prêts</p><p className="mt-6 text-sm text-zinc-500">Vos exports restent dans votre bibliothèque.</p></div></section><section className="rounded-3xl border border-white/10 bg-zinc-950/40 p-6 sm:p-8"><div className="flex items-center justify-between"><div><h2 className="font-serif text-2xl">Vos dernières idées</h2><p className="mt-1 text-sm text-zinc-500">Suivez vos générations en temps réel.</p></div><Link href="/bibliotheque" className="text-sm text-amber-200">Tout voir</Link></div><div className="mt-7 space-y-3">{isLoading?<p className="text-sm text-zinc-500">Chargement de vos créations…</p>:data?.generations.length?data.generations.map(g=><div key={g.id} className="flex flex-col gap-3 rounded-2xl border border-white/10 bg-white/[.025] p-4 sm:flex-row sm:items-center"><div className="grid h-11 w-11 place-items-center rounded-xl bg-violet-500/15 text-violet-200"><Music2 className="h-5 w-5" /></div><div className="flex-1"><p className="font-medium">{g.title}</p><p className="mt-1 text-xs text-zinc-500">{g.style} · {g.durationSeconds}s · {new Date(g.createdAt).toLocaleDateString("fr-FR")}</p></div><Badge variant="outline" className="w-fit border-white/10 capitalize text-zinc-300">{g.status}</Badge></div>):<div className="rounded-2xl border border-dashed border-white/15 p-8 text-center"><p className="font-medium">Votre bibliothèque attend sa première vibration.</p><p className="mt-2 text-sm text-zinc-500">Commencez par décrire une ambiance, un tempo ou une histoire.</p><Link href="/creer"><Button variant="outline" className="mt-5 rounded-full border-white/15 bg-white/[.03] text-white hover:bg-white/10 hover:text-white">Créer un morceau</Button></Link></div>}</div></section></div></DashboardLayout>;
}
