import DashboardLayout from "@/components/DashboardLayout"; import { Button } from "@/components/ui/button"; import { trpc } from "@/lib/trpc"; import { Download, Music2, Play } from "lucide-react"; import { Link } from "wouter";
export default function Library(){const {data,isLoading}=trpc.dashboard.library.useQuery();return <DashboardLayout><div className="mx-auto max-w-5xl"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-amber-300">Bibliothèque</p><h1 className="mt-3 font-serif text-4xl tracking-[-.04em]">Vos morceaux, à votre rythme.</h1></div><Link href="/creer"><Button className="rounded-full bg-amber-300 text-zinc-950 hover:bg-amber-200">Créer un morceau</Button></Link></div><div className="mt-10 grid gap-4 sm:grid-cols-2">{isLoading?<p className="text-zinc-500">Chargement…</p>:data?.length?data.map(g=><div className="rounded-3xl border border-white/10 bg-white/[.025] p-5" key={g.id}><div className="flex justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-violet-500/15 text-violet-200"><Music2 className="h-5 w-5"/></span><span className="text-xs capitalize text-zinc-500">{g.status}</span></div><h2 className="mt-7 font-medium">{g.title}</h2><p className="mt-1 text-sm text-zinc-500">{g.style} · {g.mood} · {g.durationSeconds}s</p>{g.status==="completed"?<div className="mt-5 flex gap-2"><Button size="sm" className="rounded-full bg-white text-zinc-950 hover:bg-amber-200"><Play className="mr-2 h-3.5 w-3.5"/>Écouter</Button><Button size="sm" variant="outline" className="rounded-full border-white/10 bg-transparent text-white hover:bg-white/10 hover:text-white"><Download className="mr-2 h-3.5 w-3.5"/>Télécharger</Button></div>:<p className="mt-5 text-xs text-amber-200">Votre création sera affichée ici dès qu’elle sera prête.</p>}</div>):<div className="col-span-full rounded-3xl border border-dashed border-white/15 p-12 text-center"><p className="font-medium">La première piste est toujours la plus belle à imaginer.</p><Link href="/creer"><Button className="mt-5 rounded-full bg-amber-300 text-zinc-950 hover:bg-amber-200">Ouvrir le studio</Button></Link></div>}</div></div></DashboardLayout>}
